<?xml version="1.0" encoding="utf-8"?>
<!--
  Deliberately minimal (compliance guide section 3.3):
  - only typeWindowStateChanged: we need "which app came to the foreground", nothing else
  - canRetrieveWindowContent="false": no screen content is ever read
  - no isAccessibilityTool flag: FocusForce+ is not an accessibility tool in Google's sense
  - packageNames are narrowed at runtime to the packages with blocking rules
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/accessibility_service_description"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:notificationTimeout="100"
    android:canRetrieveWindowContent="false"
    android:accessibilityFlags="flagDefault" />
