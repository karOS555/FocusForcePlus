package com.focusforceplus.app.ui.theme

import androidx.compose.ui.graphics.Color

// --- Primärpalette ---
val Blue700    = Color(0xFF1d4ed8)  // Primärfarbe
val Blue800    = Color(0xFF1a3a6b)  // Akzent dunkel
val Blue850    = Color(0xFF152e54)  // Akzent tiefer
val Blue900    = Color(0xFF0f2847)  // Akzent tiefst

// --- Hintergrund & Oberflächen ---
val Background = Color(0xFF050508)  // Fast Schwarz
val Surface    = Color(0xFF0f1419)  // Leicht aufgehellt
val Surface2   = Color(0xFF141c24)  // Karten-Hintergrund
val Surface3   = Color(0xFF1a2535)  // Erhöhte Oberflächen

// --- On-Colors ---
val OnPrimary    = Color(0xFFFFFFFF)
val OnBackground = Color(0xFFe2e8f0)  // Helles Blaugrau
val OnSurface    = Color(0xFFcbd5e1)
val OnSurfaceVar = Color(0xFF94a3b8)  // Gedimmter Text

// --- Zustände ---
val PrimaryContainer    = Color(0xFF1a3a6b)
val OnPrimaryContainer  = Color(0xFFbfdbfe)
val SecondaryContainer  = Color(0xFF152e54)
val OnSecondaryContainer = Color(0xFF93c5fd)

// --- Fehler & Warnungen ---
val Error        = Color(0xFFf87171)  // Rot, lesbar auf dunklem BG
val OnError      = Color(0xFF7f1d1d)
val ErrorContainer    = Color(0xFF450a0a)
val OnErrorContainer  = Color(0xFFfecaca)
val Warning      = Color(0xFFfbbf24)
val WarningContainer  = Color(0xFF451a03)

// --- Umrisse ---
val Outline        = Color(0xFF334155)
val OutlineVariant = Color(0xFF1e293b)
